---
title: A small game
---

import interactiveScene from "../../snippets/08-interactive-scene.tsx?raw"

# A small game

You've now seen every piece. Time to put them together into something that
actually plays.

<Demo code={interactiveScene} />

A 3x3 grid of cubes. One lights up at a time. Click the lit one before time
runs out — every hit scores a point and lights up a new cube. You have 20
seconds.

That's a real game, written in fewer than 130 lines of `solid-three`. And
the only thing about it that's specifically 3D is the choice to use
`<T.Mesh>` instead of `<button>`.

## What's actually in here

Reading top-to-bottom:

- **Game state** lives in four `createSignal`s — `running`, `score`,
  `activeIndex`, `timeLeft`. They're the same primitives you'd use for any
  Solid app.
- **The grid** is built once outside the component as a list of positions.
  We render the cubes with `<For>` — Solid's keyed list primitive, working
  exactly the way it does in any DOM tree.
- **Each `<Cube>`** is a custom component (the composition pattern from
  [chapter 8](/tutorial/07-custom-components)). It uses `useFrame` to bob
  the active cube forward, and `onClick` with `event.stopPropagation()` to
  score.
- **The countdown** is a regular `setInterval` driving the `timeLeft`
  signal. The DOM panel reads it the same way the scene's `active` prop
  reads its signal — both are just Solid views over the same source of
  truth.
- **`<Show>`** swaps the "start" / "play again" button in and out — the
  same control-flow component you'd use for a modal or a toggle anywhere
  else.

## That's the tour

You've now seen the whole library — the foundational pieces, the
reactivity primitives, the event system, the loader/lifecycle utilities,
the composition patterns, and how they assemble into a real interactive
scene. From here on, the way to learn more is to build something. The
[API reference](/api) covers anything we didn't touch; the rest is just
three.js.

If you want a look at where the library is heading next, the encore is a
peek at WebGPU.
