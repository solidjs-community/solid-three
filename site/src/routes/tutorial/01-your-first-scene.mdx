---
title: Your first scene
---

import createTSnippet from "../../snippets/01-create-t.tsx?raw"

# Your first scene

Mounting a `<Canvas>` is the whole boilerplate. It wires up a renderer,
a camera, and a scene — all the three.js machinery you'd normally
construct by hand — and gives you back a JSX tree to fill.

```tsx
import { Canvas } from "solid-three"

export default () => <Canvas />
```

Empty for now. Everything else — cubes, lights, cameras — lives as
children of it.

To put a `three.js` class into that tree, hand a namespace of classes to
`createT`. It returns a proxy where every key is its own JSX component.

<Demo code={createTSnippet} />

The colour comes from `MeshNormalMaterial`, which tints each face by its
surface normal — a handy debugging material because it makes geometry
visible without any lights.

`T.Mesh`, `T.BoxGeometry`, `T.MeshNormalMaterial` — every class in the
`THREE` namespace becomes available the same way. We'll pass the full
namespace throughout this tutorial because it's easier to follow when
imports aren't the point. (In real apps you'd hand `createT` a curated
object to keep your bundle small; see
[the API reference](/api/components/t) for the details.)

Three new ideas snuck into that snippet — `createT`, the `T.*` namespace,
and the way `<T.Mesh>` consumes its children as a geometry and a material.
We unpack the last one in the next chapter, where nesting becomes the
scene graph.
